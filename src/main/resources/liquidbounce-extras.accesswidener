accessWidener	v1	official

# LiquidBounce's own access widener does NOT propagate to add-ons - every entry there is a plain
# `accessible`/`mutable`, not `transitive-`. Widen anything you need yourself, here.

# AutoSign reads which sign the editor was opened for.
accessible field net/minecraft/client/gui/screens/inventory/AbstractSignEditScreen sign Lnet/minecraft/world/level/block/entity/SignBlockEntity;
accessible field net/minecraft/client/gui/screens/inventory/AbstractSignEditScreen slot Lnet/minecraft/world/level/block/entity/SignTextSlot;
